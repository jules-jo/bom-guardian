run = ["sh", "-c", "pip install -q -r requirements.txt && streamlit run app.py --server.address 0.0.0.0 --server.port 8080 --server.headless true"]
modules = ["python-3.12"]

[env]
PYTHONUNBUFFERED = "1"

[[ports]]
localPort = 8080
externalPort = 80
